version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: vercel_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgre
    ports:
      - "5433:5432"